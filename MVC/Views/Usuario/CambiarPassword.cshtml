@model MVC.Models.Usuarios.CambioPasswordViewModel

@{
    ViewData["Title"] = "CambiarPassword";
}

@ViewBag.Mensaje

<h1>CambiarPassword</h1>

<h4>CambioPasswordViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CambiarPassword">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Password" class="control-label"></label>
                <input asp-for="Password" class="form-control" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NuevaPassword" class="control-label"></label>
                <input asp-for="NuevaPassword" class="form-control" />
                <span asp-validation-for="NuevaPassword" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
